import React from "react";

const Layout = ({ children }) => {
  return (
    <div>
      {/* You can add a header, nav, or footer here */}
      {children}
    </div>
  );
};

export default Layout;
